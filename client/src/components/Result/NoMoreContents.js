import React from 'react';
import { Result, Icon } from 'antd';

const NoMoreContents = () => (
  <Result
    icon={<Icon type='meh' theme='twoTone' />}
    title='No more contents!'
  />
);

export default NoMoreContents;
